import Ember from 'ember';
// import ColorThief from 'npm:color-thief';

export function colorThief(sourceImage/*, hash*/) {
  // var colorThief = new ColorThief();
  // console.log(colorThief.getColor(sourceImage));
  // return colorThief.getColor(sourceImage);
}

export default Ember.Helper.helper(colorThief);
