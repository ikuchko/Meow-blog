import { colorThief } from 'meow/helpers/color-thief';
import { module, test } from 'qunit';

module('Unit | Helper | color thief');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colorThief([42]);
  assert.ok(result);
});
