import { rating } from 'meow/helpers/rating';
import { module, test } from 'qunit';

module('Unit | Helper | rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rating([42]);
  assert.ok(result);
});
