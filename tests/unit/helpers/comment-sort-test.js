import { commentSort } from 'meow/helpers/comment-sort';
import { module, test } from 'qunit';

module('Unit | Helper | comment sort');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commentSort([42]);
  assert.ok(result);
});
